
## macOS Environment
- Use `pbcopy`/`pbpaste` for clipboard operations
- Use `open` command to open files/URLs with the default application
- Homebrew (`brew`) is the standard package manager — prefer it for installing system dependencies
